# Browser Safety Contract

Allowed without final confirmation: inspect pages, map fields, draft answers, fill reversible text fields and upload a user-selected resume when requested.

Always stop for the user at login challenges, CAPTCHA, identity verification, legal declarations, irreversible account changes and the final submit/apply action.

Never evade platform controls, fabricate eligibility, or silently overwrite a populated field whose value conflicts with the candidate profile.
